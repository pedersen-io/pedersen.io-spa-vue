apiVersion: v1
kind: Service
metadata:
  name: {{ include "dp-spa-vue.fullname" . }}
  labels:
    {{- include "dp-spa-vue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dp-spa-vue.selectorLabels" . | nindent 4 }}
