apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dp-spa-vue.fullname" . }}-test-connection"
  labels:
{{ include "dp-spa-vue.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dp-spa-vue.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
